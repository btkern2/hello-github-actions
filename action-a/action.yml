name: "Hello Actions"
description: "Greet someone"
author: "autocat@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    defualt: "World"
    
  runs:
    using: "docker"
    image: "Dockerfile"
    
  branding:
    icon: "mic"
    color: "purple"
    
